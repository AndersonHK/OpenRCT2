# Upstream migration: independent oversight

Maintained by the supervising task, separately from the migration worker's journal. Reviews use frozen commits so an in-progress port cannot change the evidence. An active 20-minute heartbeat checks progress and alignment; routine healthy checks need no notification. This document is not a source of new owner decisions.

## Review 01 — 2026-09-14 UTC

- Reviewed baseline `e874ceb7708974e00040419d30390686c52a5d31` through U016 receipt `543cbdfa279ef9fb12f6762c05bb8e715d21f622`: 16 of 361 source commits accounted for, 345 remaining against frozen target `15d4b5e933555913d216f4548f673cd55cfb0579`.
- Worker task: **catching up to upstream**, ID `01a09df2-5b4a-7ab3-a32c-5d1d5e2a83b9`; branch `codex/catching-up-to-upstream`. Work continued during review: U017 receipt `9f0411fb13f741c9b5137ef1bc2cee21487e62be` appeared, with 344 remaining. Its implementation belongs to the next review range.

### Accounting and migration shape

Independent Git checks passed for all 16 reviewed receipts: ledger order matches second-parent source commits; each receipt incorporates exactly one previously unreachable source commit; remaining count decreases by exactly one; every receipt contains its source trailer. Unrelated Vulkan documentation was excluded from every receipt. No history rewriting was requested.

Reviewed the source diff for invalid ride-type rejection, Best Staff eligibility, terrain-edge blending, release/dependency metadata, and ride-cheat tab moves including shop/facility previews. Changes are localized to their historical source patches, with fork-specific UI/action adaptations recorded. The moved ride-type callback retains construction-ghost reset logic; the maze operating option remains present. No additional concrete source defect was found in this review. Interactive rendering remains unverified.

Independent checks confirmed all 42 private fork string symbols moved from 7039–7080 to 8000–8041 with identical English text/placeholders. The mapping and compatibility consequence for plugins using private numeric IDs are documented by the worker. An initial reviewer check looked only in the core string header; the corrected check includes the UI header where the fork symbols reside and passes.

`RideRatings.cpp`, `Ride.cpp`, `Guest.cpp`, `Park.cpp`, and `Station.cpp` are byte-identical to baseline at U016. The reviewed interval therefore preserves these implementations of sampled ratings, ride value/age pricing, guest behavior, park growth, and station behavior. This is a checkpoint observation, not a guarantee about later ports.

### O01 — independent protocol revision policy: correction requested

**Status: closed in Review 02; corrected by U018.** U002 advanced the fork protocol revision from 3 to 4, but U008 (`fe03c6d1f0703eddbd7770a1fbb674dbfe19f3c0`) reset it to upstream's zero. The worker documented a rationale based on the distinct release version and retained `andersonhk` flavor. That avoids an immediate identity collision with the earlier release, but departs from the approved port plan's explicit independent revision policy: “Never assign upstream's 0 or strip the flavor.”

Sent the worker a correction: retain release metadata and fork flavor, restore revision 4 (or a documented subsequent monotonic increment where warranted), preserve that policy across release commits, and record the correction with lightweight identity evidence. Do not rewrite receipts or disturb one-source-commit accounting. This implements the already approved recommendation and requires no new owner decision. The worker's working tree showed a network-file edit during review; this is not yet evidence of a committed correction.

### Validation and next review

Worker validation records report a complete Release/Vulkan/resource build with zero warnings/errors at U016 and 17 passing selected tests, following the earlier batch at U010. This is consistent with the requested batching; the supervisor did not duplicate those builds/tests. Independently verified Git accounting and string/preserved-file checks above are separate from those reported results.

Tracked limitations remain: Best Staff boundary behavior, terrain edges in four rotations, ride-tab layouts and live cheat toggling, and facility preview composition. Existing local object conflicts constrain presentation claims. None justifies per-commit full builds or deployment. Review the next completed range beginning U017, verify O01 in committed code and the journal, and examine the upcoming broad mechanical changes for accidental loss of fork modifications.

## Review 02 — 2026-09-14, 04:17 UTC heartbeat

Frozen checkpoint: U036, `8a80e52197a864e6ee65ed6498035ac9fb1bc3e6`; **36/361 accounted for, 325 remaining**. Reviewed U017–U036. Worker remains active; unrelated Vulkan documents and this oversight document remain outside its commits.

- **O01 resolved:** U018 (`2886403bc27edc170a91dc16422a5b5e65d2b6d9`) restores revision 4, retains `andersonhk`, and adds a source comment prohibiting upstream release resets of this counter. The migration and validation journals explicitly acknowledge the earlier deviation and correction. Confirmed the corrected code persists at U036.
- **Accounting:** independently rechecked all 36 receipts against ledger order, exact singleton source ancestry and source trailers. Remaining count is 325. No masked multi-commit ancestry step was found.
- **U017 conversion:** reviewed the actual patch. Custom objects are selected through existing `GetPackableObjects` unless `--strip-objects` is requested; the path exporter now forwards that selection alongside the existing fork `TargetVersion`. No save-version or gameplay-policy replacement accompanies the port. Runtime conversion round trips remain pending.
- **U018 include cleanup:** inspected fork-sensitive object manager and paint headers. A check of every changed source line found only include/forward-declaration/comment changes apart from the documented revision correction. No runtime implementation was replaced by the large include patch.
- **U019–U021 UI:** reviewed visibility helpers, input/draw guards, fork pricing and rating-leg controls, longitudinal-G graph visibility, dropdown release polarity, and toolbar changes. Purposeful price-control type switching remains intact. The fork leg selector retains stay-open behavior, while fare/admission selectors retain auto-close behavior; touch handling retains the equivalent inverse condition. U021's no-product-change disposition is supported by the debug visibility condition already retained during U019. No new concrete defect found in this sampled review; it does not replace interactive checks across window state transitions.
- **U023–U035 renames:** independently token-compared every changed source/test file in all 13 receipts: **181 file-deltas, 119 distinct substitutions**, with no unexpected tokens. Permitted differences were identifier case changes and `Default` to `standard`, plus whitespace. String literals, numbers, operators, enum order and implementation bodies were preserved. This supports preservation of fork vehicle/audio/construction behavior despite the broad diff. Ratings, park and station implementation files are also byte-identical to the previous checkpoint.
- **Validation:** inspected the Batch 04 build log's successful exit summary (zero warnings/errors) and test log's 16 passing tests. The naming batch is awaiting its next compile checkpoint; no duplicate build requested. Conversion round trips and the previously listed interactive/award/terrain checks remain explicit validation debt.

No new owner decision or corrective request from this interval. Next review starts at U037 and continues checking mechanical changes against fork implementations; O01 stays closed unless a later release resets the counter again.

## Owner correction — dependency ownership, 2026-09-14

**O02: direction corrected by owner; implementation verification pending.** At 04:19 UTC the worker reported one failing test among 45 selected tests: the shipped kart object lacked the fork's expected -6 dB friction-sound gain. At 04:21 it traced this to the downloaded object bundle and proposed restoring the setting with a repeatable post-download patch. At 04:23 the owner directed it to use and, where needed, update the existing objects fork instead of applying that workaround. The worker accepted this direction and began removing the workaround. These statements were read from the active task's conversation; this review does not yet certify a completed dependency correction.

Independent evidence: sibling `C:\Users\imper\Documents\GitHub\OpenRCT2-objects` has origin `AndersonHK/OpenRCT2-objects` and upstream `OpenRCT2/objects`. Its commit `cc4a8cb02308218f3b1388882af5fe068df218d8` adds `frictionSoundGainDb: -6.0` to four kart definitions (RCT1 go_karts, RCT2 kart1, and Time Twister 1920racr/cavmncar). The main repository's `docs/ride-audio-loudness-research.md` explicitly describes this calibration as data-owned. `SpatialAudio.ShippedKartDefinitionProvidesFrictionSoundGain` checks the loaded object. U006's `assets.json` instead selected the upstream v1.7.11 archive.

The failure mode is broader than losing one constant: a locally plausible fix can make a test pass while bypassing the repository that owns the feature and leaving other fork data exposed to replacement. An upstream-to-upstream release comparison cannot establish preservation of our fork's asset changes. A checksum establishes artifact integrity, not correct dependency selection. Rename-only C++ diffs do not establish behavior equivalence when runtime data changed.

**Reviewer miss:** earlier oversight accepted the upstream archive verification and classified local object conflicts mainly as a validation limitation. It should have traced object provenance and checked for a companion fork. The earlier statements remain historical evidence of the checks performed, not approval of upstream objects as the mod's dependency.

Standing review changes, also added to the recurring monitor:

- Trace behavior through its owning repository, dependency pin/build input, generated package and runtime-loaded data. Include companion repositories and assets in fork-preservation checks.
- When a regression appears, establish why the input or assumption changed before accepting a repair. Scrutinize output-patching steps, duplicate defaults, changed test expectations and shims that compensate for the wrong dependency or legacy assumptions.
- Follow up on the objects-fork update/pin, removal of the proposed workaround, and targeted loaded-object evidence for preserved calibration. Respect dirty source data and distinguish source changes from generated packages. Keep validation batched; no additional owner decision is required for the remedy already directed.

## Review 03 — 2026-09-14, 04:47 UTC heartbeat

Frozen engine checkpoint: U048, `2229c6e46acbede1dd46c7ad72f477bbd048a853`; **48/361 accounted for, 313 remaining**. Reviewed U037–U048 and the companion dependency correction. Worker is active.

**O02 source ownership correction verified.** `assets.json` now identifies `AndersonHK/OpenRCT2-objects`, local checkout and pinned receipt `b2a5511cd7ff90646dd40c61f08cb27fa977e8f4`. MSBuild, CMake/macOS and Android paths invoke the same installer; the calibration workaround is absent from the committed changes. The installer verifies the checkout revision and refuses tracked object edits. Four companion source receipts account for its four missing commits independently of the engine's 361. Independent checks verified all four singleton ancestry steps and byte-identical calibrated kart files against companion baseline `cc4a8cb0`. Inspected the Batch 06 log showing all 45 selected tests pass, including loaded kart calibration. The correction's runtime evidence therefore goes beyond merely validating an archive checksum.

The pin is currently local and the journal explicitly records that publishing the companion revision is required before other users can fetch it. No publication requested by this review. Exporter build and non-Windows runtime limitations remain documented.

**O03 open — mixed object destinations can still succeed.** At this checkpoint `scripts/install-fork-objects.py` copies/compares tracked files but does not check whether an old packed object with the same ID remains in the destination. Thus the local cleanup fixes the reviewed development install, while another existing packed installation can retain competing definitions and receive a success message. The journal's clean-destination requirement is not enforced by the installer. Sent the worker a request for non-destructive conflict detection or another provenance-aware solution, preserving unmanaged assets; a targeted old-packed/new-JSON fixture is sufficient. This is a follow-up to the already directed dependency remedy, not a new owner choice.

Also sent a small source-backed cleanup: dependency edits corrupted the CMake `DEBUG_LEVEL` help text from `0–3` to `0â€“3`. Restore its encoding or use ASCII `0-3` in a subsequent receipt.

**Engine review:** all 48 receipts independently pass ledger order, singleton source ancestry and source-trailer checks. U037 adjusts Appearance colour controls; U039 restores concrete widget types for aliased banking/speed controls following the visibility migration; U046 guards a missing station object only in its caption helper. The other reviewed source work moves headers/globals or translations and records merge receipts. Independently confirmed the complete moved WindowFlags declaration and all 20 widget-index constants retain their definitions/values. No additional gameplay-policy drift found in this interval. U039 also reinforces the need for transition checks on aliased widgets; the generic hidden-widget test alone did not cover that path.

Next review begins at U049, verifies O03 and CMake text correction in committed code, and checks the UI-header batch outcome. No redundant build or deployment requested.

## Review 04 — 2026-09-14, 05:21 UTC heartbeat

Frozen checkpoint: U094, `052d74247f7f9fbeb13c0daeef96762167f42a63`; **94/361 accounted for, 267 remaining**. Reviewed U049–U094. Worker remains active. Independently verified all 46 new receipts against ledger order, singleton upstream ancestry and source trailers.

**O03 closed at U049 (`ca39d23303`).** The installer now scans source and destination IDs before copying, rejects competing packed/loose definitions, and records installed paths so subsequent updates detect stale managed assets. It preserves unrelated music packs and performs no automatic deletion. Inspected the implementation and independently ran its four targeted fixtures: all passed (0.028 seconds), covering competing packed kart preservation, unrelated music preservation, stale managed asset rejection and updating an expected loose path. The CMake help-text corruption is also corrected to `0-3`. No open correction remains from Review 03.

**Construction/UI:** U049 separates speed controls from banking widget aliases. Reviewed independent indices, the mask-bound assertion, dispatch, holdability, visibility and resize paths. Speed limits/increments, cheat minimum zero and selected-track action dispatch are retained. The source commit also changes global spinner/dropdown heights from 12 to 14; this broader presentation effect is explicitly recorded as visual debt. U050 bundles macOS icon assets and metadata while retaining ICNS fallback; macOS runtime appearance is not verified here. U051 is localisation.

**Renames:** token-compared every source/test file delta in U052–U094: 343 file-deltas, 196 distinct substitutions. All are case changes, `Default` to `standard`, or the two explicitly inspected U061 transformations `maze_type` to `MazeType` and `entrance_or_exit` to `entranceOrExit`. No non-identifier token changes were found. The fork maze step-rating call remains in the reviewed function. Ratings, park implementation and the objects dependency manifest are unchanged in this review range.

**Validation:** Batch 07 reports the header checkpoint build and 27 selected tests passing; subsequent construction and naming batches are recorded through U083. Inspected the Batch 11 build log (success, zero warnings/errors) and Batch 10 test result (51 passing). Batch 09's initial missing relative park fixture was resolved by running the identical binary/filter from `bin`, with no source or expectation change. Compilation for U084–U094 remains deferred to the next coherent checkpoint. No repeated full build requested by oversight.

Confirmed user-requested annotated tag `stable-before-upstream-2026-09-13` resolves to baseline `e874ceb7708974e00040419d30390686c52a5d31` (primary branch `develop`). No new owner decision or corrective request. Next review begins U095.

## Review 05 — 2026-09-14, 05:43 UTC heartbeat

Frozen checkpoint: U114, `f7ac048b68a050524e98ced537d51008bcc37a34`; **114/361 accounted for, 247 remaining**. Worker active on the next header changes. All 20 new receipts independently pass ledger order, singleton ancestry and source-trailer checks.

Token-compared all 314 source/test file-deltas in the U095–U105 and U107 naming commits: 177 distinct identifier substitutions, no numeric/string/operator changes. The only substitution outside the expected naming normalization was U102's `Normal` to `rowing`; inspected its boat-state definition and consumers directly and confirmed unchanged ordinal and logic. Ratings modifier renaming reaches the fork aggregate-rating dispatch without replacing formulas, coefficients or the removed maze-size bonus. U106 adds the scoped-enum naming rules to `.clang-tidy`; no runtime effect. U108 is a merge receipt.

Independently compared the combined U109–U113 header cleanup across 188 source paths after excluding includes, simple forward declarations, empty declaration namespaces and whitespace. The sole residual was removal of a templated FlagHolder forward declaration in RCT2.h, also inspected directly. No runtime expression changes found. The worker's missing TrackData declaration was repaired by restoring the direct include; no rating code was changed to accommodate the build.

Inspected Batch 14's successful rebuild log (zero warnings/errors) and all 154 passing selected tests. Batch 12/13 also document ratings/pathfinding/import and network/state coverage. Clang-tidy and non-Windows builds remain explicitly unverified. The companion object pin is unchanged, and the independent network revision remains intact. No new finding, owner decision or worker correction; next review begins U115.

## Review 06 — 2026-09-14, 06:05 UTC heartbeat

Frozen checkpoint: U128, `3a15714082b3b6f83482ced6de73f471f53719cf`; **128/361 accounted for, 233 remaining**. Worker active. Independently verified all 14 new receipts in ledger order with singleton ancestry and source trailers.

**U115 large namespace move:** independently token-compared nine large Ride/Vehicle implementation files after removing comments, namespace wrappers/qualifications and include lines. Remaining differences were three redundant global qualifiers and three `Ajustment` to `Adjustment` function spelling corrections; no formula, timing, state or motion expression changes. Independently compared the complete Ride and Vehicle struct bodies, including fork fields, defaults and inline methods: identical after namespace/whitespace normalization. Extracted VehicleFlags retains the complete declaration and uint32 storage. This directly addresses the risk of copying large upstream bodies over fork implementations.

**Flags and headers:** reviewed U119's inverted JSON flag behavior, U120's wall bit order/byte storage and colour aliases, and U121's scroll flags/uint16 storage. No change found to missing slope-property defaults or equivalent flag operations. Worker records exhaustive reset-mask equivalence and successful Batch 17 tests. U116–U117 header ownership fixes and U127–U128 include cleanup are recorded with retained fork dependencies and explicit non-Windows validation limits.

**U124 approved D10 behavior:** inspected clear-action changes and UI defaults. Small=1, walls=8 and path additions=16 remain independent; default UI selects small plus walls. The addition removal uses the existing nested action with inherited flags and avoids dereferencing the prior element after successful path deletion. Existing ghost filtering and the fork's restart-after-erasure loop remain intact. The stream revision correctly advances 4 to 5; the API-117 change is intentional, with no legacy-mask shim. Inspected the test log showing all 18 selected tests pass; its all-32-mask fixture covers serialization, query non-mutation, cost agreement and ghost/adjacent-wall cases. Positive-cost insufficient-funds combinations, live staff claims, embedded park scripts and interactive presentation remain explicit debt, not claimed coverage.

U126 correctly records the existing 1G reset as already satisfied without a further protocol bump. No new drift or owner decision found. Next review begins U129; continue scrutinizing large moves and remaining gameplay changes.

## Review 07 — 2026-09-14, 06:28 UTC heartbeat

Frozen checkpoint: U135, `a68f07dd91f6f145b4989f40f1e94e1a1393993b`; **135/361 accounted for, 226 remaining**. Worker active on the next flag refactor. Independently verified all seven new receipts against ledger order, singleton ancestry and source trailers.

U129–U132's combined changes across 87 source paths independently compare equal after removing include/forward-declaration changes and whitespace/comments. The fork PathElement bin helper retained its required direct header after the batch compile exposed it. U133's merge disposition documents the previously handled 1G/protocol difference rather than copying its tree.

Reviewed U134's five clear-scenery flag positions, predicates and explicit uint8 visitor/serialization payload. The D10 mask meanings and ghost/mutation safeguards survive the type change; no additional protocol bump is warranted.

For U135, independently verified all **32** peep flag positions against the old masks, including bit 31 and the renamed unknown bit 21, plus all **27** script flag names against their corresponding masks. Compared the complete ParkFile.cpp token stream: apart from the peep field becoming its uint32 holder reference, it is unchanged. Inspected action serialization, script get/set operations, the Easter-egg conditional setters, short-circuit random calls, freeze handling, fork interaction mask and fare/route condition changes. No restoration of upstream park-rating logic or change to the fork's transport economics was found. Packed legacy source flags remain raw uint32.

Inspected Batch 21's log showing **151 selected tests passing**; the journal records a full build with zero warnings/errors and the smaller clear-mask checkpoint before it. No new correction or owner decision. Existing live/visual and non-Windows validation debt remains; next review begins U136.

## Review 08 — 2026-09-14, 06:50 UTC heartbeat

Frozen checkpoint: U143, `ccb38d4f6ae6c7723b96147ac35f8ec885c51cb0`; **143/361 accounted for, 218 remaining**. Worker active. All eight new receipts independently pass ordered singleton ancestry and source-trailer checks.

**U136 park flags:** reviewed uint64 modern storage, packed uint32 RCT1 storage and the unchanged raw RCT2 source field. Independently confirmed the complete park serializer changes only its flags argument to the uint64 holder. All 21 defined modern flag positions are below bit 32. The journal explicitly records that temporary backups/unset operations now preserve unused upper bits previously truncated by uint32 intermediates. This changes preservation of undefined bits, not a defined gameplay option; legacy free-entry/no-money and interest translations remain documented. Batch 22 reports 176 selected tests passing after an uncommitted syntax-conversion error was repaired without weakening tests.

**U138 D03:** inspected the three complete action changes. Only water-specific placement/floating-structure/water-height restrictions move behind disableClearanceChecks; missing-surface checks and other validation remain outside the new guards. Revision 5 to 6 correctly reflects changed action validity. The worker's new loaded-boat/real-track test exercises normal and cheat query/execute paths, with topology regressions recorded separately. Off-water ride operation and preview rendering remain unverified.

**U139 wall sounds:** reviewed legacy two-bit extraction, separate sound field, retained flags2 positions, JSON aliases, explicit reset and the XXWLBR03 correction. Invalid values now select no sound, avoiding unchecked table access. The worker clearly records this malformed-object behavior decision and tested all 256 legacy bytes plus representative valid/invalid JSON values. Inspected the final log: all 51 selected tests pass. The authoritative companion definitions were checked by the worker and required no repin; valid 0/1/2 sound behavior remains the contract.

**U140–U142:** reviewed vehicle enum widths, JSON defaults and legacy spinning-car restriction. Independently token-compared all 92 RTD registry/definition files: exactly 94 k-prefix identifier substitutions and no other token changes, preserving every fork descriptor value/callback. These commits await their stated compile checkpoint. U137/U143 are ancestry receipts. No new correction or owner decision; next review begins U144.

## Review 09 — 2026-09-14, 07:12 UTC heartbeat

Frozen checkpoint: U155, `f420db6abd56fcaf88923b65611cf8eeb7ecf181`; **155/361 accounted for, 206 remaining**. Worker active. All 12 new receipts independently pass ordered singleton ancestry and source-trailer checks.

Independently compared fork/source token changes for U144 (40 files, 31 CarEntry names), U146 (four files, one sprite-group method), U154 (11 files, 26 motion identifiers) and U155 (24 fork files, eight tile-element names). Every changed identifier pair matches the actual upstream patch, with no other token changes. U145 additionally introduces a harmless trailing initializer comma; sprite-group strings remain unchanged. This supports preservation of fork car properties, platform loading references, acceleration/braking/station logic and safe copied-element banner cleanup. Inspected U155's serialized padding references and unchanged array widths/size assertion; its compile checkpoint is still pending.

U148 introduces the map.resize hook at the end of successful execution, after the fork topology reset, park size update and UI invalidation. Reviewed signed size/shift payload construction and mutable callback flag. API 118 and independent protocol revision 7 reflect the new capability. Successful no-op/shift-only notifications are explicitly documented; query emits none. Inspected the 32-test passing log; the new real-plugin regression checks notification count, payloads, completed map size and mutation. Multiplayer/replay synchronization and no-scripting builds remain validation debt.

U149 intentionally uses sensor-landscape orientation for Android launch/game activities and removes SDL's overriding orientation request; native Android validation is not claimed. U150–U152 retain live fork UI dependencies while excluding deleted OpenGL implementations. The worker records its incorrect initial ObjectEntryIndex declaration rationale and the subsequent header correction, with receipts preserved. Inspected Batch 27's successful zero-warning/error build and **147 passing selected tests**; Batch 25 cleared earlier U140–U144 debt. No new finding or owner decision; next review begins U156.

## Review 10 — 2026-09-14, 07:35 UTC heartbeat

Frozen checkpoint: U163, `060a4bf13790f2d6488316a62f71d0ad232cd26e`; **163/361 accounted for, 198 remaining**. Worker active, with the U160–U163 build in progress at snapshot. All eight new receipts independently pass ordered singleton ancestry and source-trailer checks.

Independently compared all **351 source/test file-deltas** across these eight tile-element ports against the identifier pairs in their actual source patches. After the two explicitly inspected exceptions below, no non-identifier token changes remained. The only fork identifier pairs outside their individual source patches were the three U160 receiver corrections recorded in the journal.

- U158 removes an unused, undefined LargeSceneryElement GetObject declaration; no matching implementation was found in the prior source. No object-loading implementation was deleted.
- U161 replaces an inline surface-object lookup with its existing getter. Inspected both: same context, object manager, surface index and returned pointer, with the grass-growth null check retained. All other grass timing, randomness, water-height units and ownership expressions compare unchanged.
- U160 restores a WallElement accessor prematurely renamed during U156 and two RCT12 source accessors renamed during U158/U159. Modern destination accessors retain their new names. Inspected the importer corrections; legacy types and values were not changed to accommodate the error. Subsequent worker passes now use more discriminating receiver contexts.

The token comparisons cover packed fields, masks, photo/brake/maze data, station and seat/door accessors, safe erasure/copy cleanup and fork-specific consumers; no upstream implementation replacement found. Inspected Batch 28's **227 passing tests**, which clear U155–U159 after the corrections. The journal correctly excludes U160 and later names from that result. No new correction or owner decision; next review begins U164 and checks Batch 29.

## Review 11 — 2026-09-14, 07:57 UTC heartbeat

Frozen checkpoint: U176, `c4ef1030819f49d399157b30dd6993d5d35a88ed`; **176/361 accounted for, 185 remaining**. Worker active. Independently verified all 13 new ordered singleton receipts and source trailers.

U164 records two missed surface-accessor spelling corrections used by the fork presentation snapshot. Batch 29 reports 254 tests passing and the journal explicitly retains live-owner object resolution before background presentation. The combined U165–U169 header changes across 96 source/project paths were independently checked: no implementation tokens changed after excluding includes, declarations and the unchanged int16 WindowNumber alias; the project entry/deleted forwarding header are separately documented. Inspected Batch 30's successful zero-warning/error build.

U171 intentionally increases the shared button-face height to 14 and aligns save controls; the journal traces the resulting fork graph/marketing layout effects. U172 is localisation. U173/U174 preserve entrance byte values and ordered centre/left/right placement while typing storage and topology references. Reviewed the public script setters: object type clamps uint32 before narrowing, whereas sequence narrows to uint8 before clamping. Their different behavior for 256/257 is accurately documented and covered by real-plugin boundary tests. Both retain `Invalidate(data, true)`; invalid loaded bytes remain untouched, and the fork topology guard still rejects out-of-range rows. Protocol revisions 8/9 account for changed script mutations; API118/save60016 remain.

U175/U176 only replace private bit-0 operations with uint8 holders. Inspected masks, setters, packed fields and the accounted-marker method; no alteration to traversal, costs, path selection or unknown bits. Inspected Batch 31's **240 passing tests**, covering through U173. U174–U176 await B32; interactive layouts, multiplayer/replay and non-Windows debt is recorded. Ratings implementation and companion pin are unchanged in this interval. No new correction or owner decision; next review begins U177.

## Review 12 — 2026-09-14, owner-requested blocker review

Frozen checkpoint: U184, `cb64f95e31ba14103e784a4bee1fb0433742c11b`; **184/361 accounted for, 177 remaining**. Independently verified all eight U177–U184 receipts against ledger order, singleton upstream ancestry and source trailers. Worker is idle at U185 pending the owner's external ownership contract decision; no U185 production edit or receipt exists. Monitoring paused during the decision as configured.

Reviewed the complete U177–U179 production delta: path and track flags preserve bits 0..6, scenery support preserves bit 0, and packed fields remain uint8 holders. The fork watering redraw is untouched. U181's raw indestructibility getter and relocated removal cheat predicate preserve removal eligibility; a complete consumer search confirms the inspector can now display and clear the actual stored flag while the cheat is active. U183 only extends hover invalidation to hiddenButton widget type. U184 assigns ride visibility to existing cheat permission and advances fork protocol 9 to 10, retaining the fork flavor. Inspected passing log tails for Batch 32 (240 tests), the corrected Batch 33 fixture (one test; the journal records the other 146 passing earlier), and Batch 34 (16 tests). No new completed-port correction found; live UI/multiplayer and non-Windows coverage remains unverified.

Independently inspected U185's actual ownership getter/setter, script boundary, action parameter visitor, serialization and cost predicate. The pricing regression is real: the upstream negation now encompasses both operands, charging for transitions between unowned/for-sale masks. Reviewed the 256-pair arithmetic model and its 12 zero-to-land-price differences; compiled action coverage remains pending. Preserve the old purchase predicate under the existing economics policy.

The external-number decision is legitimate and distinct from pricing: old land-owned value 32 becomes 2, and writing 32 through the new script setter clears the stored ownership nibble. Save storage itself remains unchanged. The approved plan explicitly preserves external numeric contracts, so accepting this break requires an owner decision. The worker's API119 recommendation is coherent for upstream plugin compatibility, but existing/embedded ownership-writing plugins are not inventoried. Public TypeScript property/action documentation also needs explicit values under whichever contract is selected. User is reviewing the decision note directly; do not infer approval or resume U185 until answered.

### U185 owner resolution — 2026-09-14

Owner approved upstream ownership API/action numbers: plugins target upstream, so adopt compatible contracts when they do not change intended fork gameplay. Informed the migration task to resume its existing goal at U185, consistently use 1/2/4/8, document public values, increment fork protocol independently to 11, preserve packed save/fence layout and retain the old land-purchase predicate. No legacy-number adapter is required. The 20-minute oversight monitor is active again with this decision included; do not reopen it.

## Review 13 — 2026-09-14, 08:45 UTC heartbeat

Frozen checkpoint: U192, `611589b1fb94a1e9bf62b2c2a3de88b127db4c38`; **192/361 accounted for, 169 remaining**. Worker active. Independently verified all eight U185-U192 receipts against ordered ledger entries, singleton upstream ancestry and source trailers.

U185 implements the owner's decision consistently: public script/action values use 1/2/4/8, API119 and independent protocol11 retain the fork identity, packed ownership remains in the high nibble, and both TypeScript contracts document the change. Compared changed source lines against upstream across 27 production files: only the approved land-price predicate and fork protocol differ. Reviewed the actual 256-transition action test, independent four-by-four legacy cost table, wire byte and query nonmutation assertions, and packed-byte preservation coverage. The pricing correction survives U186/U187. U186's 11-file change matches source; U187's differences are the retained pricing correction and an updated comment, with names retaining their bit positions.

U188's eligibility move retains its predicates and null-as-empty fallback. Independently inspected TileElementsView's start, terminal handling and unfiltered iteration: no ghost exclusion or mutation is introduced. The localized scenario helper retains ownership/fence updates. Source and fork rating code and the authoritative companion objects pin are unchanged in this interval.

Reviewed complete U190-U192 production deltas. Toolbar bounds now follow the centered button group; the fork's additional width check handles GUI resize resetting width even when rounded x stays unchanged. Title menu changes use the attention-flashing lookup while retaining existing open/close branches. Image upload catches standard exceptions and releases its acquired JS data reference on failure; allocating replacement pixels before freeing the old image avoids leaving the existing image dangling on allocation failure. Conversion policies, 300-pixel dimension limit and renderer invalidation remain. Allocation failure itself is not injected; interactive rain/alignment/title attention and live multiplayer remain validation debt.

Inspected actual passing log tails: Batch35 245 tests, Batch36 127 tests, Batch37 23 tests. The journal records successful bounded Windows builds and clearly limits UI-binding test coverage to the MSBuild configuration. No new correction or owner decision. Next review begins U193.

## Review 14 — 2026-09-14, 09:07 UTC heartbeat

Frozen checkpoint: U198, `3c6def531a1805fda0d4398b712c7ce82e20a012`; **198/361 accounted for, 163 remaining**. Worker active on uncommitted U199. All six new receipts independently pass ordered singleton ancestry and source-trailer checks.

**O04 — open, correction sent:** U195 omitted `InvalidateByClass(WindowClass::parkInfoPanel)` from `INTENT_ACTION_UPDATE_PARK_RATING` in WindowManager.cpp. The actual upstream source 8dab2cce07 contains it; the fork retains only parkInformation invalidation after removing the former toolbar dirty flag. Park::SetForcedRating and normal rating updates broadcast this intent, while the new panel has no periodic invalidation. Rating display can depend on an unrelated redraw. Sent source-backed instruction to restore the missing line in a subsequent receipt, record the U195 audit correction and add narrow real-window-manager intent coverage in the next batch. No owner decision required; preserve ongoing U199 work. Verify the committed fix and validation at next review.

U193's colour extraction preserves scenario versus utility RNG, draw order, bounds and the 32-tick preview cycle. U194 is exactly the Unix include restoration, with native validation still outstanding. U195's new park/news panels match source; the date panel preserves GameTime::SecondsToTicks(30) and intentionally fixes its initial right edge. Inspected null-safe panel resize, title/editor centering, open/close shortcuts, unchanged cent-money formatting and unused window class slots 3/4. Theme conversion remains explicitly due at U340. Aside from O04, reviewed owner adaptations are coherent; interactive appearance remains unverified.

Independently token-compared all 43 U197 fork source/test files against the preceding fork commit: only 47 identifier pairs from the actual upstream change occur after excluding comments and the removed unused Paint declarations. Punctuation, numeric values, strings and field order are unchanged, including fork registry, batched park serialization and train tests. U198's ten redraw retargetings match panel ownership; the console date correction intentionally improves on upstream's incorrect park-panel target. U198 awaits its next batch.

Inspected passing log tails for Batch38 (77 tests after a fixture setup correction) and Batch39 (240 tests). Ratings implementation, protocol11 and companion asset pin are unchanged in this interval. Next review begins U199 and follows O04.

## Review 15 — 2026-09-14, 09:30 UTC heartbeat

Frozen checkpoint: U203, `e0f8987dcd718c028711eb16eb1f8071e3edac6c`; **203/361 accounted for, 158 remaining**. Worker active on uncommitted U204 path-navigation API; that work is outside this checkpoint. All five new ordered singleton receipts and source trailers independently verified.

**O04 closed in code:** U199 `e2b2eb9053` restores the exact missing parkInfoPanel invalidation. Independently compared all five climate/guest-count/rating/date/cash intent bodies to the U195 source: they now match. The journal corrects its prior completeness claim and identifies/removes the scratch emitter's erroneous global duplicate-insertion shortcut. A runtime dirty-region test was not added because the headless fixture lacks a drawing engine; this limitation is explicit and proportionate to the one-line correction. Actual rendered redraw remains part of interactive validation debt.

Independently token-compared all 58 U199 fork source/test files, excluding only the separately inspected WindowManager correction. All changes are the 89 identifier pairs found in the actual source (86 member/method pairs plus three locals renamed to prevent shadowing). Numeric tokens, strings, punctuation, types and ordering are unchanged. The comparison preserves C++ digit separators, avoiding the worker scanner's initial mistake. Reviewed packed peep fields, inline movement, batched park serialization, fork transport/platform references and legacy receiver boundaries. Batch40's final zero-warning/error build and 252 passing tests follow the three corrected station references; no test expectation was weakened.

U200/U203's five translated files exactly match their source blobs. U201 only moves dependency includes behind ENABLE_SCRIPTING and retains enabled-code contents/order; standalone disabled syntax coverage is documented without claiming a full disabled build. U202 moves widget refresh into prepare-draw without changing pickup eligibility. Inspected the actual guest-window test: repeated state changes and all three fork entering-ride platform substates update the pickup flag while dimensions remain fixed, then walking re-enables it. Batch41's final zero-warning/error build and all 49 selected tests pass; rendered appearance remains untested.

No new correction or owner decision. Ratings implementation, protocol11 and companion objects pin are unchanged. Goal metadata still reports the old U185 blocked state, but the worker is actively continuing under explicit owner authorization; monitor for an idle stop and resume work if needed. Next review begins U204, emphasizing the approved physical-path API contract and preservation of fare-aware guest routing.

## Review 16 — 2026-09-14, 09:52 UTC heartbeat and owner-requested overall review

Frozen checkpoint U210, `123d6bb6af4202c520a98801915d4f6de6a83814`: **210/361 accounted for, 151 remaining**. Worker continued into U211 during review. All seven U204-U210 ordered singleton receipts/source trailers independently verified. Full stable-baseline assessment is in `docs/upstream-migration-cohesion-review-2026-09-14.md`.

**O05 open:** U204 reference revisions permanently invalidate live paths during render-only ScopedTileIndexOverride and TrackDesignDrawPreview stash/restore. Construction preview substitutions affect real tiles near (128,128); full preview stashing invalidates all live navigators. Source-backed correction sent: restore original live identity after balanced temporary views while expiring preview-acquired references, retaining monotonic allocation and real mutation invalidation. Working edits implement revision stash/restore and scoped restoration; do not certify until committed and tested. No owner decision required.

Otherwise the new physical navigator preserves reviewed upstream traversal (including first-match, directional/slope/banner/option rules) and remains separate from guest fare/transport/topology caches. Inspected lifetime checks and mutation owners; identified O05 precisely because ownership of a temporary view differs from ownership of real structural change. U206's three moved/remaining coordinate headers match source tokens. Reviewed the 60 other C++ paths: includes/forward declarations and seven unchanged Direction=uint8_t aliases account for all remaining differences; no implementation changes. U205 startup-folder guard, U207 unfocused quit attention, U208 Ctrl estimate and U210 resolved wall Z retain their stated scopes. Inspected B42/B43/B44 logs: 276/279/99 passing selected tests.

Stable comparison supports cohesion: exact function tokens for ride-age value, sampled local-context environment/scoring and happiness park rating; cent-money/40-TPS headers, MapTopology.cpp, PresentationGeneration.h and all 28 Vulkan files byte-unchanged. Pricing/growth formulas retain numerical policy across flag changes. No existing test names disappeared in changed test files; 22 cases were added. Companion content diffs are empty despite stat-cache status noise. Objects install and protocol ownership remain as corrected in earlier reviews. Full HUD/theme sequence and interactive/live multiplayer/replay/non-Windows checks remain unfinished. Requested one full available Windows test suite at the next coherent checkpoint after O05; do not duplicate full testing between commits. Next incremental review begins U211 and verifies O05. User reports reactivating the goal; do not treat the earlier blocked metadata as an unresolved owner decision.

## Review 17 — 2026-09-14, 10:20 UTC heartbeat

Frozen checkpoint: U215, `c44799a5c817dabcde9bed517160076e6cb9877e`; **215/361 accounted for, 146 remaining**. Worker active on U216. All five new ordered singleton receipts/source trailers independently verified; U215 locale blobs match their source.

**O05 closed in U212 `65c3da4795`:** inspected committed save/restore of each overridden tile revision and the stashed live-map revision array, with no allocator rewind. The new actual-plugin test exercises all five construction-preview coordinates, nested/repeated scopes, retained connections, expired temporary references and permanent invalidation after real replacement. Full-map preview test restores live references while keeping temporary references invalid. Independently inspected unfiltered B46 XML/log: **585 tests, 50 suites, zero failures/errors/disabled tests**. This run covered O05 before the subsequent U212 downloader edits; final combined U212 evidence is separately **108 selected tests passing**. Cohesion review updated with this resolution. Actual rendered preview and live multiplayer remain unverified.

U211 preserves separate visibility state while honoring explicit false/true/unspecified image-button borders; inspected descriptor parsing and all runtime image/border accessors, including legacy image-index conversion. Its real-plugin/raw-widget coverage is reported accurately without claiming visual testing.

U212 replaces the unsafe shared-future approach with the fork's existing owned BackgroundWorker. Read the complete controller and worker dispatch/cancellation/shutdown implementation: background requests capture values, canceled completions are discarded, and controller/repository writes run in main-thread completion. Context shutdown joins before service teardown. Reviewed cancellation/restart/destruction tests, failure progression and successful-install-only reporting. The short-DAT guard precedes the repository's unchecked header read. Uses the configured object repository and existing legacy lookup service; no companion dependency fallback. In-flight HTTP still cannot be aborted and can delay final context shutdown, as documented; no live HTTP/native successful installation is claimed.

U213 annotations and explicit ignored future preserve existing server-list worker waiting; the separate legacy window-close wait is not disguised as fixed. U213 compilation awaits the next batch. U214 is a supported merge receipt. No new correction or owner decision. Assets pin, protocol11 and ratings implementation unchanged in this interval. Next review begins U216.

## Review 18 — 2026-09-14, 10:45 UTC heartbeat

Frozen checkpoint U234, `8fe36a66dc15a106f930443e20e3fad46f08f9be`: **234/361 accounted for, 127 remaining**. Worker active beyond this checkpoint. All 19 ordered singleton receipts, ledger source IDs and remaining-count trailers independently verified. U219/U224 source trees equal their merged second-parent trees; fork receipts contain documentation only.

U216 correctly places the resolved result-Z assignment outside the automatic-height conditional in both Query and Execute, avoiding upstream's explicit-height reporting regression. Read the seven independent placement cases, including raised objects over ground/water, and verified no placement/cost/clearance implementation changed. U217/U218 only extend missing-object labels and window layering; the owned downloader lifecycle remains intact.

U220 implements approved D05. Read the full terrain generator and actual upstream/fork UI patches; generator is source-exact except the inspected singleton safety guard. Terrain reads feed existing fork actions; cash aggregation, action ordering and topology owners remain intact. The three tests cover both axes/directions, spike filtering without terrain edits, singleton handling and cross-section heights. Correcting an unnormalized fixture matches the production normalized-range contract. Upstream's algorithm does not guarantee connected/buildable output on every cliff or irregular cross-section. Actual modifier transitions, invalid-cursor/release sequences and generated geometry through placement actions remain explicit validation debt, not completed evidence.

U221's palette implementation and both U222 picked-up peep functions independently match their pre-port fork bodies after whitespace normalization. U223 retains those draw/invalidate bodies under the private API, including assertions and the copied render target. Its position setter preserves the fork's zero-zoom fallback and null viewport guard. No legacy renderer mutation was restored. U225/U229 identifier token comparisons and U230-U234 inspected constant diffs preserve values/arithmetic. U227 translation text matches source; only the source's extra trailing blank line is omitted.

U228 adopts approved D08 with upstream conversion/mutability/bounds/update/tweener ordering. Read UpdateTrackChange, tweener removal, full-payload publication and the two actual API tests. Explicit publication uses the existing owned presentation queue even when coordinates do not change. Seat/sample fields survive the tested lane change; no sample reset, guest-routing rewrite or extra tick is introduced. This does not prove live boarding, later directed-leg continuation or rendered pose behavior. Upstream numeric conversion quirks are documented without an added compatibility shim.

Inspected B47-B50 build tails (zero warnings/errors) and XML: B47 46 pass; B48 initially 73/74 then all three new geometry tests pass after fixture correction; B49 80 pass; B50 initially 81/82 then both new setter tests pass after correcting cross-realm error identification. No production weakening to satisfy those fixtures. U229-U234 compilation remains batched debt. Assets pin, ratings, protocol, cent-money and timing policy paths are unchanged in this interval. No new correction or owner decision; next review begins U235.

## Review 19 — 2026-09-14, 11:09 UTC heartbeat

Frozen checkpoint U252, `ad8486aed052ca167d2864fe0c99bde071b1a00b`: **252/361 accounted for, 109 remaining**. Worker active. All 18 ordered singleton receipts, source-ledger IDs and count trailers independently verified. U239 source equals its merged parent; U242 has no combined resolution delta and its constituent loader changes were inspected. U239/U242/U244/U247 fork receipts contain documentation only, with already-covered dispositions supported by code.

Independently compared complete production added/removed-line sequences to actual upstream for U235-U238, U240/U241, U245/U246/U248, U250-U252: all match. U243's 15 existing files are unchanged after stripping includes; the retired OpenGL path is appropriately omitted. Constant refactors preserve station capacity, hooks, map allocation and scaled-PI arithmetic. U244's direct PaletteIndex include was already verified in U221.

U240 official path mappings, custom legacy single-object paths and station loading use the existing configured ObjectManager. Reviewed the fallback distinction: a missing custom path switches to modern surface/railing fallback, while a loaded legacy path needs no separate railing. Preview loading remains manager-only with existing map stash/restore. U241's ride-map rebuild reads ride objects only; non-ride registration can safely skip it. No asset pin, installer or repository-precedence change.

U249 retains captured adjacent vehicles. Independently read the complete painter, its regression and ViewportPaint/JobPool barriers: foreground paint work is joined before subsequent simulation work, while entity snapshots can be older than live ride metadata. The added stack/RAII/thread-local guard prevents exact same-session vehicle re-entry without altering acyclic delegation. The test reproduces a captured two-proxy cycle despite a newer positive live count and verifies repeated calls unwind the guard. This supports crash suppression, not visual equivalence of normal boats; the journal states that limitation. No live-entity fallback or gameplay mutation was introduced.

U245's inclusive title geometry and four-configuration repeated Finances-tab regression are coherent. U247 preserves independent optional HUD widths rather than overlapping a surviving panel. U246 removes an undocumented invalid-image sentinel; U248 changes the tab's representative car only. U250 keeps selection masks 1/4/16/32 and packed track masks 03/1C/E0/F0; both old clear callers used all flags. U251 invalidates a cached preview index after list reload, preserving lazy loading.

Inspected B51-B55 zero-warning/error build tails and XML: 80/66/51/48/61 selected tests pass respectively, with overlapping coverage not summed. U251 compilation can remain in the next batch. Ratings, protocol, asset pin, cent-money and timing policy paths are unchanged. Interactive previews/normal boat rendering, non-Windows and live multiplayer/replay remain validation debt. No new correction or owner decision. Next review begins U253.

## Review 20 — 2026-09-14, 11:32 UTC heartbeat

Frozen checkpoint U269, `d99bbfa52bc3672f30b34d6773ba75fb747504f1`: **269/361 accounted for, 92 remaining**. Worker active. All 17 ordered singleton receipts, source-ledger IDs and remaining-count trailers independently verified. Complete production patch sequences match source for U253, U255-U260, U264 and U268/U269 (ignoring trailing whitespace). The additional U254 assertion-format adaptation uses the strong ride ID's underlying integer correctly.

Read the new set/clear-name operations and actual scenario tests. Only addressed custom names change; full SHA equality and scenario-only S4/S6 importer gates remain. U258's CD ownership data independently equals the previously approved Okinawa patch: 111 owned, 4 rights-owned, 5 rights-for-sale and 94 land-for-sale coordinates. Tests exercise all 214 typed ownership results and name clearing without changing car count/accrued ticks, but use synthetic terrain/rides rather than original scenario imports. No price/growth or general ownership rule was changed.

U262 sanitizes packed-object storage names through the existing platform sanitizer, retains the configured user-object directory, collision suffixes and payload bytes, and does not modify internal IDs or dependency provenance. Inspected its real repository storage test and the explicitly documented invalid-ZIP payload limitation. Existing Platform.cpp tests were missing from Windows registration; the worker added registration and reran them, correcting the initially empty filter claim rather than counting absent coverage.

U264's station flags retain the same five bits, uint32 storage and JSON keys. The extra fork boarding consumer initially missed by the source-only conversion is corrected in U265 with the exact former null-style/noPlatforms predicate; independently searched and found no remaining old constants. This was compile debt fixed within the batch, with journal claims corrected. U266 is already covered by the fork's combined vertical/small-curve visibility expression; verified both buttons receive that OR once. No station allocation or track capabilities were replaced.

Independently verified all 1,014 U267 font manifest entries retain order/metadata and referenced image bytes, with all 690 renamed paths preserving decimal-to-Unicode identity and style. This checks the intended mapping rather than trusting Git's pairing of identical images. U268/U269 glyphs and manifests exactly match their upstream blobs, and their enum/lookup insertions and Won label were inspected. Font archive regeneration and rendered glyph/index validation remain scheduled with B61; do not treat current source checks as a rebuilt asset result.

Inspected successful B56-B60 build tails and XML: 47/3/5 selected tests, B59's separate 35 existing plus 2 newly registered platform tests, and B60's 39 tests pass. Counts overlap and are not summed as unique coverage. Asset pin, ratings, protocol, cent-money and timing policy paths remain unchanged. No new correction or owner decision; next review begins U270 and follows the font archive checkpoint. Original scenario imports, actual station/font rendering, non-Windows and live multiplayer/replay remain outstanding validation categories.

## Review 21 — 2026-09-14, 11:55 UTC heartbeat

Frozen checkpoint U278, `8670fdff468a3500d39ad959c92d517494621986`: **278/361 accounted for, 83 remaining**. Worker active. All nine ordered singleton receipts, ledger IDs and count trailers independently verified. U273 has no combined resolution delta; U275 is a documentation-only already-covered receipt supported by the existing hide-only network visibility switch after editor/settings visibility.

Font follow-up closed for the planned build/lookup scope: independently verified U270's 15 currency path moves retain all manifest metadata/order and image bytes; U271/U272 glyphs and manifests match source blobs. Inspected actual loaded-glyph test: all three styles distinguish lowercase hard sign, Won and Hryvnia from their former/fallback slots, load positive-width entries and check guilder offsets. B61 final XML reports 24 tests passing; B62 reports 10. Current built fonts.dat has 1,023 entries, consistent with the final manifest. Earlier fixture failures are not treated as passing coverage. Actual text rendering/TTF/package validation remains separate.

U274 editor split: independently compared all twelve navigation/save callback bodies to the old fork implementation; exact after whitespace and callback-name normalization. Read controller visibility/click guards, status labels, opening lifecycle, scene registration, resize and shortcut paths. Existing theme classes are reused, invalid/hidden/decorative clicks cannot dispatch callbacks, and repeated opening does not duplicate panels. The actual UI-context regression exercises all nine steps, invalid step, entity-dependent visibility, a real backward transition and registered toolbar toggling. Save dialogs and rendered themes remain untested. U275 cannot re-show editor-hidden controls because the retained network switch only hides.

U276's entire clearance function body exactly matches its pre-port fork body after removing additionalInfo member qualifiers. This preserves blocked/null/elementErased handling and retry behavior. All ten other production file edit sequences match source; independently read action argument grouping/defaults. No safe-mutation implementation was restored from upstream. U277's three production file edit sequences also match source and only add error titles/string metadata.

U278: independently verified all sixteen scenario JSON payloads against source, 64-digit hashes against filenames, 36 appended set_name operations, and every existing non-ride property unchanged. The names are fixed official strings, not localized defaults. Previously verified full-hash/scenario-import gates remain; synthetic tests do not establish identities in the original scenario files.

B62-B65 build tails show zero warnings/errors. B63/B64/B65 test logs report 52/41/38 passing tests respectively; these are overlapping selected suites. Asset pin, ratings, protocol, cent-money and timing policy paths remain unchanged. No new correction or owner decision. Next review begins U279; original scenario imports, rendered editor/font behavior, non-Windows and live multiplayer/replay remain standing validation debt.

## Review 22 — 2026-09-14, 12:18 UTC heartbeat

Frozen checkpoint U286, `87f9d8a9f5d4fad7c4143bd410134ba6f3f2f6b5`: **286/361 accounted for, 75 remaining**. Worker active. All eight ordered singleton receipts, ledger source IDs and remaining-count trailers independently verified.

U279 independently token-checked against source: source 471 substitutions in 99 files; fork 576 substitutions in 86 files. Every changed token is an identifier pair present in the actual source; values, operators, ordering and storage layout are unchanged. Fork snapshot interfaces and GetEntityForPresentation remain separate from renamed live-registry calls. Inspected B66 XML/build: **603 tests in 53 suites, zero failures/errors/disabled**, 37.778 seconds, with no filter; zero-warning/error build. This full checkpoint covers U279, not the later plugin/input commits below.

U280's 84 changed translations independently match source. Only formatter change is Spanish STR_1858 CURRENCY to CURRENCY2DP, consistent with cents. U281 is the CD wording correction. U282's complete ScObject.hpp matches source; inspected base-class finalization, derived prototype construction, class registration/teardown and actual six-object/GC test. Derived getters move to shared prototypes and lose own-property presence while remaining non-enumerable/configurable. This observable compatibility consequence is correctly recorded; no getter body or dependency lookup was replaced.

U283's production guards exactly match source. Read StopBegin/interval/socket/StopEnd ordering and actual synchronous close-handler test. Stopping plugins cannot create the new timer/connection, while synchronous cleanup still executes; inert wrapper creation and broader socket lifetime are explicitly outside the demonstrated fix. No network request was needed for the regression.

U284's adaptation correctly distinguishes UTF-8 byte SelectionStart from codepoint Length: signed clamping uses buffer bytes, snaps away from continuation bytes and clears selection. Read focus/session/buffer checks and actual multi-byte, negative/end offset, two-widget, blink and closed-session tests. This preserves valid ASCII behavior without imposing a character-count interpretation on an existing byte cursor. Actual OS editing/IME and rendered caret-width limitations remain untested.

U285/U286 production edit sequences exactly match source. The queued-input regression checks horizontal grab-point bounds, outside dropdown dismissal and parent-closed cleanup through the real input dispatch; synthetic SDL setup does not prove OS capture or multi-monitor behavior. Inspected B67-B70 zero-warning/error build tails and final XML: 22/12/24/25 selected tests pass respectively, with fixture corrections documented and no weakened production behavior.

Asset pin, ratings, protocol, cent-money and timing policy paths remain unchanged. No new correction or owner decision. Next review begins U287. Native rendering/input, same-build multiplayer/replay, longer performance runs, non-Windows and full disabled-scripting builds remain standing validation debt.

## Review 23 — 2026-09-14, 12:40 UTC heartbeat

Frozen checkpoint U296, `abaed358a16927cdc7d790d9eff41d9fa1e8f629`: **296/361 accounted for, 65 remaining**. Worker active. All ten ordered singleton receipts, ledger IDs and count trailers independently verified.

U287-U289 production code-line deltas match source. Independently parsed all 197 Unicode and 34 CS constants: numeric values, declaration order and normalized names remain identical. All 194 font mapping rows and 32 legacy conversion rows retain order/targets across the enum conversions. Read the bsearch comparator, typed defaults, Windows enum unwrapping and ICU code-page mapping. Existing Windows wide-character narrowing remains; this is not a surrogate-pair fix or serialized-layout migration. U290 is the documented merge receipt.

U291 independent token proof confirms 82 source and 57 fork identifier substitutions across 13 files, exclusively source rename pairs. Fork moving-entity compaction, presentation-only tween positioning and Context restore/reset paths are unchanged. No legacy interpolation implementation was reintroduced. U292/U295's 3/13 translation additions match source; U293 removes only inspected warning directives and associated comments, with no runtime replacement.

U294's observable text change is recorded accurately: small close glyph U+2715 uses the legacy AD sprite, U+274C uses ASCII X, and legacy byte AD now decodes to U+2715. Inspected the TTF sprite-fallback branch, both colour/size string variants and actual loaded-font/legacy decoder/widget tests. No obsolete string ID was renumbered. Native TTF pixels remain unverified.

U296's six code-line multisets match source; Guest.cpp diff grouping differs because of pre-existing fork spacing, so this was supplemented by inspecting the actual effect, lookup and intensity bodies. All 24 name strings/effects retain order; station bonuses remain +35/+25/+55/+14 and Mr Bean speed 9. The only getEasterEggNameId consumer is the station switch, where none=255 preserves the former -1 no-match no-op. The enum is computed, not stored in Guest. IntensityRange keeps the same uint8/nibble layout, clamping and fork happiness/preference logic under renamed accessors.

B71-B75 build tails show zero warnings/errors; XML confirms 39/47/4/27/51 selected tests pass respectively. Save/import and scripting coverage is included in B72/B75, with individual name-effect/native interpolation tests still outside that evidence. Protected ratings, asset pin, protocol, cent-money and timing paths are unchanged. No new correction or owner decision. Next review begins U297; live multiplayer/replay, native rendering/input, non-Windows and full disabled-scripting remain standing validation debt.

## Completion handoff — owner instruction, 2026-09-14

The owner authorized final validation and deployment to `D:\Games\Independent\OpenRCT2Mod` after all 361 entries are integrated, followed by revising `docs/vulkan-exclusive-migration-plan.md` against that deployed version. The worker has been asked to finish ports and the final Windows build/batch tests, with exact commit and artifact paths. Oversight owns final independent review, deployment and plan revision. The recurring monitor now includes these follow-ups. Merging into main/develop remains conditional on subsequent owner manual testing; no merge, push or Vulkan implementation is authorized by this handoff.

Preparation at U307 (not a new completed source review): the existing `scripts/windows/deploy-local.ps1` copies release documentation and plugin declarations from a separate base installation and mirrors `bin/data` using `/MIR`. Do not use it unchanged for this catch-up: package documentation/declarations from the completed source/build, inventory destination-only assets before replacement, preserve user data and a rollback copy, and verify the installed object provenance and hashes. `bin/data/object` currently has four junctions into the companion source; deployment must materialize their pinned contents rather than leave development-checkout dependencies or silently skip them. The installed destination uses ordinary object directories and lacks the new provenance marker. A clean staging inventory plus explicit reconciliation is needed to avoid retaining competing legacy object packs. Existing `backup-before-deploy-20260702-063438` must be preserved. No destination files were changed during preparation.

The exclusive Vulkan plan still names the pre-catch-up baseline. Revise it only after deployment, recording the deployed commit, current source locations, newly affected rendering/plugin/font/HUD paths, and unresolved native/GPU/offscreen validation. Automated suite success is not evidence that manual gameplay or rendered output is regression-free. Last independently reviewed source remains U296; continue from U297.

## Review 24 — 2026-09-14, 13:21 UTC heartbeat

Frozen checkpoint U317, `b8f2a83fbf413af49d313cf828fcc27723cf1009`: **317/361 accounted for, 44 remaining**. Worker active on the next palette refactor; no concurrent implementation edits or builds from oversight. Independently verified all 21 ordered singleton receipts, ledger source IDs and remaining-count trailers. U302/U304/U312 have no merge resolution delta. The initial remerge audit emitted temporary-object-directory errors despite returning success; repeated with a writable isolated object directory and the existing objects as read-only alternates, obtaining empty diffs without errors. Corrected the reusable audit to reject such stderr errors.

U297: read actual source and fork draw/upload paths and the four-pass plugin/pixel regression. The additional same-size raw-image adaptation consistently applies draw transparency while preserving other metadata and final invalidation; raw uploads without a subsequent draw retain opaque behavior. GPU display remains unverified. U303's extra correction addresses the actual resource lifetime exposed by the unfiltered suite: loaded vectors are sized from headers, but unloading clears the vectors while retaining header counts. Using vector size in G2/palette/font/track/CSG lookup preserves loaded indices and prevents access after unload; CSG retains its loaded guard. The regression unloads actual resources and checks nonzero indices. It does not hide the issue with test ordering or additional asset loading. Release metadata advances to 0.5.5 while the independent fork protocol remains preserved.

U306/U307/U308/U315 independent complete-file token proofs reproduce source/fork substitution counts of 28/31, 151/151, 85/85 and 391/526 respectively. Every changed token is a source identifier pair; all other code tokens remain. This explicitly covers additional fork consumers and preserves live/stable station statistics, sampled ratings, platform/routing state, serialization and safe map handling. PatrolArea::clear's failure to reset tileCount is pre-existing in both implementations and remains documented in B79 for separate investigation, not silently corrected in a rename port.

U298/U299/U300/U301/U305/U313 source edit multisets match. U309's ordered array/scalar dispatch matches the source with the existing fork formatter conversion retained; inspected dry-run placement and actual array/name/state tests. U310/U311 complete data files match source, all patch SHA-256 prefixes/lengths agree, and every pre-existing non-ride JSON property is retained, including Okinawa ownership. These scenario name changes do not substitute gameplay payloads. Original scenario imports remain outside the synthetic fixtures.

U314's complete production edit multiset matches source; all three preview paths now apply remap flags from CarEntry while preserving frame selection and existing fork controls. U316's five-file delta matches source; fallback main/wave/sparkle IDs and flags zero preserve former null behavior, including no ducks. No new worker-held object reference is introduced. U317's renamed header is byte-identical; its compilation checkpoint is correctly deferred to the following palette extraction.

Independently read B77's unfiltered log and XML: **609 tests in 53 suites passed**, 55.078 seconds, zero failures/errors/disabled. B76-final/B78-B84 selected XML reports 51/41/56/4/4/57/170/79 passing tests, with B83's separately matched save/import run passing 7; these overlap and must not be summed as distinct coverage. Inspected zero-warning/error build tails for the lifetime correction, station refactor and latest water checkpoint. Whitespace check passed for the frozen range. No new correction or owner decision. Next review begins U318. Native rendering/input, custom-image Vulkan output, original scenario imports, long-park performance, live multiplayer/replay, non-Windows and full disabled-scripting builds remain standing validation debt. Final deployment and Vulkan plan revision remain pending completion of the source loop.

## Review 25 — 2026-09-14, 13:44 UTC heartbeat

Frozen checkpoint U333, `393a6cb2917e8b5ff57b814baa19ccc93bcc8164`: **333/361 accounted for, 28 remaining**. Worker active; independently verified all 16 ordered singleton source receipts and count trailers. U319/U324/U333 remerge diffs are empty using the isolated writable object directory. U334 was being recorded during this review and is outside the frozen checkpoint.

U318/U320/U321/U322: independently extracted and compared the complete function bodies against their prior fork locations. All four palette bodies, three screen bodies, four line bodies and ImageId catalogue body preserve every token, allowing only redundant Drawing namespace qualification in the screen move. The new Palette.cpp/.h match source. U320 retains the fork viewport-skip declaration in NewDrawing.h and does not restore removed renderer-selection APIs. U323's Rectangle files match source; the moved pixel filter retains the same single-coordinate inclusive rectangle operation. These are ownership/file moves, not substitutions of legacy rendering implementations.

U326 complete-file token proof independently reproduces 625 source substitutions across 122 files and 748 fork substitutions across 128 files, with every other code token unchanged. This covers fork audio, topology, platform/transport and serialization consumers. The final SpatialAudio diff contains only two coordinate rotations; its separate Normalise helper is preserved. No hidden arithmetic or stored-layout change. Asset pin and money policies remain unchanged, and the network changes are screen-header/call qualification only.

U325's post-onPrepareDraw bounds guard was inspected with the actual shorter-page/null-sentinel queued-input regression. Valid hover replacement remains exercised. U327's update widget was already at 0,0; inspected the actual source hunk and existing fork declaration. U328 adds only the Spanish terrain-height rejection string. U332's existing client/server switch is behaviorally equivalent to the upstream restrictions; setHidden defaults to true. Retaining the fork's visibility preparation order avoids restoring the source early editor return and does not unhide user-disabled controls.

U329/U331: the final complete ScObjectManager file matches corrected source. Read all three successful post-registration water-load refresh branches and the actual-JS regression against pinned companion cyan/green palettes. Array refresh occurs once after successful water registration; nonwater/failed load paths and research/return order remain. Independently read Vulkan SetPalette, frame publication and worker application: converted bytes are copied into the owned presentation packet, and the worker applies that packet's palette/version. The Vulkan implementation has no diff in this reviewed range. This establishes ownership continuity, not native water-pixel parity. U330 changes changelog placement only.

B85-B89 XML confirms 96/98/67/67/26 passing selected tests. B90's unfiltered log/XML reports **610 tests in 53 suites passed**, 37.956 seconds, zero failures/errors/disabled. The full run preceded restoration of the harmless separate audio-helper spelling; the corrected final rebuild has zero warnings/errors and all 26 audio tests pass. B91's corrected final build has zero warnings/errors and 63 selected tests pass; its initial failure was a fixture unload-call arity error, with production unchanged. Full-suite coverage at U326 must not be presented as an unfiltered U333 run. Whitespace check passed. No new correction or owner decision. Next review begins U334, including the already-approved station-margin deferral and remaining HUD/theme changes. Deployment and the Vulkan exclusive plan revision remain pending the completed source loop and final validation.

## Review 26 — 2026-09-14, 14:07 UTC heartbeat

Frozen checkpoint U344, `55324030459d489017f0ca895e4bbced5e16a834`: **344/361 accounted for, 17 remaining**. Worker active on the following peep display extraction. Independently verified all 11 ordered singleton receipts and count trailers. U341 has no independent merge resolution delta. U334 changes no production/release-note file; both fork station end-allowance subtractions remain, matching approved D02 rather than accepting the upstream non-block capacity increase.

U335 production edit multisets match source across all five files. Inspected water-inclusive hit selection, unchanged selection-mode ordinal, terrain/water overlay branch and patrol highlight recognition. The change allows selecting a water hit without replacing fork presentation entity lookup, cached base-surface painting, patrol mutation or staff movement. Native underwater/patrol picking remains unverified.

U336 complete Themes.cpp matches source and retains the prior ordered WindowClass references. U337/U342 source edits match; news intent retains its enum position and the corrected English track-designer label matches its intended ID. U339 complete Theme.cpp matches source at that receipt.

U338/U340: inspected actual split window implementations, predecessor/source deltas, resize owner, news dispatch, shortcut and theme import/lifecycle code. Both center windows retain independent absent-side-panel sizing; fork ride-price helper changes are equivalent string-ID renames, and DateInfoPanel retains the 30-second GameTime threshold. News queues and timing are not rewritten. Subject visibility is restored between message types, empty queues close/avoid ticker creation, singleton opening is idempotent, and hidden toolbars do not reopen from news invalidation. Restoring HUD explicitly refreshes queued news while paused. The news border uses its actual primary theme slot; swapped source editor label IDs are corrected.

Legacy theme conversion preserves byte/structured colour decoding, maps four old toolbar slots to their split owners, supplies missing-slot defaults, and preserves old editor aliases with explicit current keys taking precedence. The panel default used for a missing old first slot agrees across all three new panel descriptors. Read actual temporary theme-file tests for both versions and 0–4 slots, explicit key precedence, predefined theme switching, and live HUD resize/queue/hide-show tests. Also checked the theme-editor checkbox handler: it explicitly creates/closes the status window, while preset selection handles visibility separately. Native pixels, rain, translated text layout and actual mouse interaction are not established by these fixture assertions.

U343 independent complete-file token proof reproduces 571 source identifier substitutions across 40 files and 539 fork substitutions across 39 files, with every other token unchanged. This includes the fork GPU clipping owner without restoring OpenGL. U344's edit multisets match source in all 17 files. Read FlagHolder's bit operations, unchanged item enum, concrete all-filter list, non-terrain/non-water list, early none/label exclusion, and plugin clear/set handling. The new 16-bit representation retains selectable membership and fork presentation picking; omitted reserved bits were already rejected by the paint filter. Compile debt is explicitly deferred to the following display checkpoint.

Independently checked B92/B93/B94 zero-warning/error build tails and XML: **88/69/133 selected tests pass**, zero failures/errors/disabled, respectively. B93's actual UI/theme tests cover the recorded adaptations; B94 includes save and GPU suites. Latest unfiltered checkpoint remains B90 at U326, not this frozen HEAD. Whitespace check passed. No new correction or owner decision. Next review begins U345. Final deployment, native manual testing and Vulkan plan rebaseline remain pending completion of the source loop and final validation.

## Review 27 and final handoff — 2026-09-14, 14:31 UTC heartbeat

Reviewed U345–U361 through final source receipt `c6560d2679cd7871ba583236fd9515bb81955511`: **361/361 accounted for, zero remaining**. Independently repeated the entire historical receipt audit: exactly 361 ordered unique ledger sources, correct two parents per receipt, singleton new source reachability and correct remaining-count trailers. Stable tag still resolves to the original fork baseline. The subsequent `8617004e871a1d464d9fd7e7064b782141a94c70` handoff is documentation only.

U345/U346 retain the fork approach/wait platform guards and distinct singular/group labels. Read the full predecessor/classification switch and new formatter: it adds explicit absent-ride name arguments for every ride-bearing description, including null IDs, and preserves the documented upstream generic display fallbacks. This is presentation classification, not a stored peep state or movement change. Both platform group IDs remain inside the existing reserved range. U348's permission declaration matches its already-existing runtime owner. U349 changes no production or release-note file, retaining the approved negative-G warning.

U350 independently checked all 91 source and 91 fork descriptor files: ordered mode lists and every other non-whitespace byte are retained. RideMode declaration is byte-identical after relocation; action and dropdown membership use the same bit values/order. The proof helper was adjusted to handle two descriptors in one header; no source repair was needed. The existing unchecked >=64 mode boundary is documented separately and has not been silently hardened during this conversion.

U351–U353 source edit multisets match. Independently read the nine-type additional-zero-car lookup, S4 current/proposed count changes and shared TD4 importer, then parsed the actual nine companion object definitions: each has numEmptyCars=2. This import-only adjustment does not accept the separately deferred station-capacity change. Read the actual TD4/TD4AA stream regression and its control cases; original S4/placed-train behavior remains manual. U347/U354/U358 merge resolution diffs are empty. U355's 25 language edits match source; all non-English files match the frozen target, English IDs are unique and fork 8000–8043 are present. U356 editor visibility and U360 focus deltas match source. U359 retains currency lookup precedence, input guards and existing monetary units while changing the default for listed dollar locales.

U361's complete UiExtensions.cpp matches target. The only ScWidget.hpp difference from target is the retained U284 null-session/null-buffer caret guard. Read registration/unregistration of all ten derived prototypes and the actual all-type/GC/context-lifetime regression. Inherited properties change own-property reflection as documented; base ownership and getter/setter behavior remain. No additional owner decision is required under the approved compatibility direction.

B95/B96/B97 XML confirms 112/166/8 selected tests pass. Final B98 log/XML independently confirms **616 tests in 53 suites pass**, zero failures/errors/disabled/skipped. Its 41 GPU foundation tests and actual hidden-window Vulkan lifecycle/indexed-readback test pass; that is narrower than full visible UI/park parity. Final source and post-receipt builds have zero warnings/errors, with outputs current and source unchanged. Earlier tests' sandbox-denied user-profile persistence remains an explicit limit. The final audit initially encountered Windows default text decoding; reran with explicit UTF-8 successfully. No failing audit result was treated as proof.

Final outside review found no remaining migration blocker. [Deployment](upstream-deployment-2026-09-14.md) completed at 14:40:04 UTC with a verified rollback copy, all 4,703 installed files hash-matched, 4,454 companion object files verified and installed CLI smoke check passed. The four kart gains remain -6 dB in their actual nested car definitions. The Vulkan exclusive plan was revised after deployment; software-removal/parity gates remain open and use the accepted post-catch-up baseline. Manual acceptance, merge and Vulkan implementation have not been performed. The recurring monitor can now stop: authorized source catch-up, validation, deployment and plan revision are complete.
