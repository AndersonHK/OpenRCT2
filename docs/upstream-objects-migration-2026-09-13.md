# Companion objects fork correction

Engine checkpoint before this correction: U037, 37 / 361 receipts, 324 remaining. Objects migration is a separate four-source ledger, not four additional engine receipts.

The owner clarified that the sibling `../OpenRCT2-objects` fork is authoritative. U006's upstream v1.7.11 bundle selection was incorrect for this fork. The engine's kart parser correctly defaults to 0 dB; four object definitions already own a -6 dB calibration. Batch 05 compiled successfully, but its initial selected test run passed 44 / 45 tests: `SpatialAudio.ShippedKartDefinitionProvidesFrictionSoundGain` found gain 1 instead of approximately 0.501187. A temporary post-download calibration workaround made that test pass, but was rejected and removed without being committed. That temporary pass is not final validation.

The companion repository is `https://github.com/AndersonHK/OpenRCT2-objects.git`. Its original master remains at `cc4a8cb02308218f3b1388882af5fe068df218d8`. Branch `codex/catching-up-to-upstream` now records all four missing upstream commits through `046ae328114fe347bee6f9f90561cd8bc24ce787`, at fork receipt `b2a5511cd7ff90646dd40c61f08cb27fa977e8f4`. Nothing was pushed. See that repository's `MIGRATION-2026-09-13.md` and individual `Upstream-Commit` trailers.

Additional decisions:

- Preserve all four existing kart calibrations as object data. Do not special-case these objects in the engine parser or patch downloaded upstream definitions.
- Adopt the English tiger typo correction and Catalan spinning-car translations, omit the translation commit's accidental English typo regression, and record its later correction as already satisfied.
- Adopt the exporter patch's actual ImageSharp version 2.1.13 (the source commit subject says 2.1.10) and its row-memory API adjustment, preserving pixel coordinates and palette behavior.
- Pin `assets.json` to the local fork receipt. MSBuild and CMake consume its tracked source assets directly, with a configurable checkout location. Git and Python 3 are required for object installation. The build neither resets nor fetches the objects checkout; a missing revision or tracked data edits produce an explicit error. This revision is currently local and must be published with the fork before others can fetch it.
- Source JSON and image assets are valid development/runtime objects. This installation path does not run the optional object exporter or build compressed parkobj release archives. It does not delete unmanaged assets. Use a clean object destination when replacing a previous packed upstream installation, to avoid duplicate IDs.

Local cleanup: 419 untracked packed upstream artifacts were mixed into the companion source tree through linked runtime paths. 418 matched the upstream release byte for byte; one differed only by the temporary kart calibration. All were backed up to engine scratch `obj/upstream-audit/objects-output-artifacts-backup.zip` and verified before individually removing those derived files. Three formatting-only edits were restored after JSON equivalence checks. Other source data was retained. Twelve separate official music packs were left in place. Git status reports many stat-cache changes on this host; content diffs, not that status count, determine tracked changes.

Validation: all four object calibrations, JSON parsing, source deltas, exact-one ancestry and whitespace checks passed during the four receipts. Release x64 MSVC/Vulkan build passed with 0 warnings/errors in 8.75 seconds. All 45 selected audio, scripting, widget and station tests passed against the companion source, including the shipped kart calibration; the previous 419 conflict warnings are absent. Logs: `obj/upstream-audit/batch-06-objects-build.log` and `batch-06-fork-objects-tests.*`.

CMake's shared install helper copied all 4,454 tracked source files into a clean scratch destination. MSBuild verified the existing linked development install without copying onto the source. Android's asset preparation also calls this helper; full Android/macOS/Linux builds have not been run. Configure alternate checkouts with MSBuild `ObjectsSourceDirectory`, CMake `OPENRCT2_OBJECTS_SOURCE`, or Android environment `OPENRCT2_OBJECTS_SOURCE`.

The exporter compiled successfully from a tracked-source copy in engine scratch using .NET SDK 9.0.318. Initial NuGet restore was network restricted; the authorized restore/build retry succeeded. Its five warnings concern the existing net6.0 target being out of support and RCT2Tools.ObjectData 1.2.3 using .NET Framework compatibility assets. Those separate dependency upgrades remain outside this four-commit port. No claim of pixel-export runtime validation is implied. Log: `obj/upstream-audit/objects-exporter-build.log`.
