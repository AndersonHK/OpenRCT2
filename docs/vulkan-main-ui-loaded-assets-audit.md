# Main UI loaded-asset input audit

Agent `/root/decode_assets`, 2026-09-19. All 54 newly captured runs pass: 18 fixture keys through frozen software UI12, current software UI15 and Vulkan UI16 / ordinary build30. All 354 captured frames match the corresponding newly loaded frozen reference exactly in indexed bytes and physical RGBA. Every frame explicitly records `assetState.rct1CsgLoaded=true` and `rct1Required=true`; no capture log contains the RCT1 fallback warning. All 18 Vulkan logs confirm validation activation and contain no VUID or synchronization hazard.

The [JSON receipt](vulkan-main-ui-loaded-assets-audit.json) pins every summary SHA-256, build receipt, log hash, raw output/report hash and selected earlier same-family summary. The inputs cover baseline; overlap, scroll and text under both incremental and full invalidation; four native terrain rotations, zoom phase, smoothing, grid and overlap; fixed-ordinal night lighting; eight precipitation states; and six palette states. Current/Vulkan incremental comparisons to frozen full-state references are retained in the per-run comparison records.

I reread both raw layers of every new capture and its selected earlier capture. **None of the 354 frames changed pixels after the CSG input correction.** This establishes that loading the original RCT1 assets does not alter these particular visible fixture outputs. It does not retroactively prove that earlier processes loaded them. There is no new divergent group to inspect, no pixel tolerance and no accepted renderer exception. Existing agent visual reviews of these unchanged images retain their original feature limits.

The distinction still matters: the earlier EverythingPark forced-camera capture visibly used fallback images and largely empty framing. Its separate corrected dense saved view and fresh-process repeats are documented in the [large-park review](vulkan-large-park-visual-review.md). That inadequate initial capture is not rehabilitated by the small-park/terrain byte comparisons here.

Fresh-process repetition under this newly asserted-loaded contract remains pending for these 18 families. Two repetitions within ordinary families prove only in-process repetition; lighting and weather are prescribed single sequences, whose adjacent states are not treated as repeats. Earlier same-build fresh repeats without the explicit CSG assertion do not substitute for this remaining gate. No broader font, weather scheduling, startup lighting, native-world, platform or performance coverage is claimed.
